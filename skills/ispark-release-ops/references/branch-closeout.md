# Branch Closeout

Before commit or PR:

- inspect status and diff
- keep unrelated changes out
- run focused validation
- update docs or release notes when required
- use an atomic conventional commit where possible

If the worktree contains unrelated user edits, leave them untouched and mention them.

