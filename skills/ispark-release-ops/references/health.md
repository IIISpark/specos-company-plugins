# Health

Health is not only "process is running".

Check the smallest meaningful indicators:

- deployment rollout status
- service health endpoint
- logs around the changed path
- user-facing smoke action
- queue/worker state when applicable
- rollback or previous version availability

If only generic health was checked, say the behavior-specific smoke remains unverified.

