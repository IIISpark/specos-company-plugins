# Browser

Before testing:

- identify the URL and environment
- start the local dev server if required
- check console errors, network failures, layout, and interaction state
- test desktop and mobile when responsive behavior matters

Do not claim "looks good" without seeing the page.

