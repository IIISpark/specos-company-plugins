# Checkpoints

For long release work, checkpoint:

- current repo/branch/commit
- artifact or image tag
- target environment
- commands already run
- verification already passed
- next action and stop rule

Use checkpoints before context switches, risky operations, or handoff to another agent.

